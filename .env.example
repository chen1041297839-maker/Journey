# 本地不需要任何密钥。默认用 data/xenia.sqlite + public/uploads。
# 部署到 Vercel 时必填 Postgres（Neon 即可），图片字节存在数据库，不依赖本机磁盘。

# DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require

# 可选：自定义共享记录 id（默认 xenia）
# XENIA_JOURNEY_ID=xenia

# 可选：本地 sqlite 路径
# XENIA_SQLITE_PATH=./data/xenia.sqlite
